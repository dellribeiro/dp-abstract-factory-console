package consoles;


public class XboxConsole extends Console {
    public XboxConsole(String model, String cpu, String memory, int resolution, int storage) {
        super(model, cpu, memory, resolution, storage);
    }
}
